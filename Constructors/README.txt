Use constructors to define class features 
    in the form of global variables
    in the form of objects

name is the class name
similar to method
doesn't return any value
no void, static just use public keyword

keyword "this"
it is used to assign the current constructor value to the gloabl variable

keyword "super"
- used to call parent class constructor
- used only in child class constructor
- should be the first statement in any constructor
- if no parameter is used calls the default constructor
  else call parameterized parent class constructor based on the parameter passed